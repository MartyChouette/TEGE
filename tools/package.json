{
  "name": "enjin-tools",
  "private": true,
  "type": "module",
  "description": "Dev tooling. check_wgsl compiles the web renderer's WGSL through Dawn, which is what a browser does at run time and what no C++ or Emscripten build can tell you. web_capture renders a served web build in headless Chrome and saves what it drew, which is the only check that sees an integration bug.",
  "scripts": {
    "check-wgsl": "node check_wgsl.mjs",
    "web-capture": "node web_capture.mjs"
  },
  "devDependencies": {
    "puppeteer-core": "^25.10.0",
    "webgpu": "^0.6.0"
  }
}