# Comprehensive Codebase Audit — 2026-02-13

**Scope:** Full Engine, Editor, Player codebase
**Method:** 5 parallel automated audits (Security, Stability, Performance, Feature Wiring, API Consistency)
**Cross-referenced against:** AUDIT_2026_02_11.md, AUDIT_2026_02_12.md, AUDIT_2026_02_12_R2.md — all findings below are NEW

---

## Summary

| Category | CRIT | HIGH | MED | LOW | Total |
|----------|------|------|-----|-----|-------|
| Security | 4 | 10 | 14 | 8 | **36** |
| Stability | 5 | 10 | 15 | 8 | **38** |
| Performance | — | P1: 6 | P2: 10 | P3: 5 | **21** |
| Feature Wiring | — | 3 | 6 | 5 | **14** |
| API Consistency | — | 4 | 8 | 31 | **43** |
| **Grand Total** | **9** | **33** | **53** | **57** | **152** |

## Fix Status

**Last verified:** 2026-02-20

**103 of 109 non-API findings fixed.** 6 remaining (1 HIGH, 2 MED, 3 LOW). API consistency: 3 fixed, 5 already resolved, 4 accepted, 31 LOW deferred.

Fixed by category:
- **Security:** 35 of 36 — All CRIT/MED/LOW fixed. S-H2 fixed (vkMapMemory checks). S-L5–L8 fixed. **Remaining:** S-H1 (RT Vulkan error checks in 4 files)
- **Stability:** 37 of 38 — All CRIT/HIGH/MED fixed. T-H3, T-M3/M5/M7-M10/M14-M15, T-L1-L3/L6 all verified fixed. **Remaining:** T-L5 (Prefab s_NextId thread safety)
- **Performance:** 20 of 21 — P-1 through P-19 + P-21 fixed. **Remaining:** P-20 (LevelStreaming GetAllEntities — runs only on chunk load, LOW impact)
- **Feature Wiring:** 13 of 14 — F-H1/H2/H3, F-M1-M6, F-L1-L4 fixed. **Remaining:** F-L5 (Water3D render integration)
- **API Consistency:** 8 of 43 resolved (3 fixed, 5 already done). 4 accepted, 31 LOW deferred.

Remaining 4 actionable:
| ID | Sev | Description | Status |
|----|-----|-------------|--------|
| S-H1 | HIGH | RT Vulkan API calls without VkResult checks (4 files) | Deferred (renderer-internals zone, no user input) |
| T-L5 | LOW | Prefab s_NextId global counter not thread-safe | Accepted (single-threaded usage) |
| P-20 | LOW | LevelStreaming GetAllEntities for diff | Accepted (runs only on chunk load) |
| F-M4 | MED | ScriptPropertyParser annotation decorators unused | Fixed — parser wired into InitScript(), [Header] added |
| F-M5 | MED | ~20 gameplay component types lack VS nodes | Fixed — 20 nodes added (Sprite2D/Light/Camera/Material/Visibility/ObjectPool/SaveData/Rigidbody) |
| F-L5 | LOW | Water3D mesh has no Vulkan render integration | Backlog |

---

## 1. SECURITY (36 findings)

### CRITICAL (4)

| ID | Description | File | Line |
|----|-------------|------|------|
| S-C1 | Remaining `std::system()` calls on Linux/macOS (HotReload, EditorLayer, FileDialog) — command injection | HotReload.cpp, EditorLayer.cpp, FileDialog.cpp | 209, 12443/25340/28413/28417, 359 |
| S-C2 | Collaborative editing: unsanitized remote operations applied to World — no auth, no enum validation on EditOpType | CollaborativeEditing.cpp | 545, 360 |
| S-C3 | RPC data size truncated via `static_cast<u16>(size)` — silent data corruption for payloads >64KB | NetworkSystem.cpp | 247, 270 |
| S-C4 | Packet header payloadSize truncation to u16 — sender silently sends corrupt packets >64KB | NetworkSystem.cpp | 881 |

### HIGH (10)

| ID | Description | File | Line |
|----|-------------|------|------|
| S-H1 | RT Vulkan API calls (vkCreateImage/AllocMemory/CreateImageView) without error checks — 4 RT files | RTAO/RTReflections/RTGI/PathTracer.cpp | 90-108 |
| S-H2 | vkMapMemory unchecked in RTPipeline + AccelerationStructure — null memcpy crash | RTPipeline.cpp, AccelerationStructure.cpp | 203, 293 |
| S-H3 | NetworkId `m_NextNetworkId` u32 wraparound to 0 — entity ID collision | NetworkSystem.cpp | 160 |
| S-H4 | Collaborative editing: no NaN/Inf validation on remote transform data | CollaborativeEditing.cpp | 520-579 |
| S-H5 | SceneSerializer `std::stoi` on JSON key without try-catch — crash on malformed scene | SceneSerializer.cpp | 2293 |
| S-H6 | SceneSerializer narrowing cast `stoi→u8` without range check | SceneSerializer.cpp | 2293 |
| S-H7 | SceneLock `std::stoull` without try-catch — crash on corrupt .enjinlock | SceneLock.cpp | 288 |
| S-H8 | `popen()` on Linux in RunGitCommand — command injection via unescaped `args` | EditorLayer.cpp | 32535 |
| S-H9 | Audio handle wraparound — ClipHandle/SoundHandle u32 overflow | SimpleAudio.cpp | 152, 193, 228 |
| S-H10 | Collaborative editing sync response — unbounded scene JSON allocation (OOM) | CollaborativeEditing.cpp | 641 |

### MEDIUM (14)

| ID | Description | File | Line |
|----|-------------|------|------|
| S-M1 | Collab editing: operation log entries carry arbitrary-length dataJson — memory growth | CollaborativeEditing.cpp | 330-367 |
| S-M2 | Collab editing: deserialized string length not capped in readString | CollaborativeEditing.cpp | 536-543 |
| S-M3 | WAV loader: bitsPerSample=0 → division by zero in duration calc | SimpleAudio.cpp | 128-132 |
| S-M4 | WAV loader: no validation on channels/sampleRate (0 = div-by-zero) | SimpleAudio.cpp | 93-94 |
| S-M5 | VOX loader: `numVoxels * 4` integer overflow → OOM | VOXLoader.cpp | 109 |
| S-M6 | Collab editing: m_PendingRemoteOps grows unbounded during sync | CollaborativeEditing.cpp | 589-592 |
| S-M7 | GameSaveSystem JSON `.get<f32>()` without type check — crash on tampered save | GameSaveSystem.cpp | 151, 154 |
| S-M8 | Newgrounds ExtractString: infinite loop on malformed JSON escaped quote at end | NewgroundsAPI.cpp | 324-327 |
| S-M9 | HTTP response: 16MB truncation passed as "success" without error flag | HTTPClient.cpp | 161 |
| S-M10 | Collab editing: log prints m_PendingRemoteOps.size() AFTER clear — always 0 | CollaborativeEditing.cpp | 655-658 |
| S-M11 | NetworkSystem: static const vector<u8> in functions — destruction order risk | NetworkSystem.cpp | 94, 624, 946 |
| S-M12 | PixelEditor sprite sheet export: integer overflow on large canvases | PixelEditor.cpp | 974 |
| S-M13 | HTML5 exporter: config.width/height injected without validation | HTML5Exporter.cpp | 151-152 |
| S-M14 | `std::getenv()` null check missing in SceneLock (UB on null → std::string) | SceneLock.cpp | 18-28 |

### LOW (8)

| ID | Description |
|----|-------------|
| S-L1 | Audio m_Clips map never cleaned on unload cycles |
| S-L2 | Collab editing peer list grows without bound (stale peers) |
| S-L3 | Network interpolation buffer grows without bound per entity |
| S-L4 | Newgrounds ExtractString does not unescape backslash sequences |
| S-L5 | FileDialog ShellEscape does not filter null bytes/control chars |
| S-L6 | VOX loader: childSize from chunk header ignored |
| S-L7 | GameSaveSystem: no slot range check (unlike SaveSystem) |
| S-L8 | Collab editing: double-free risk if callback destroys entity during conflict resolution |

---

## 2. STABILITY (38 findings)

### CRITICAL (5)

| ID | Description | File | Line |
|----|-------------|------|------|
| T-C1 | Unchecked `std::stoi` in SceneSerializer visual script deserialization — crash on malformed scene | SceneSerializer.cpp | ~2293, ~2308 |
| T-C2 | `nodeMap.at(nid)` in ShaderGraph code gen throws on missing key — crash on inconsistent graph | ShaderGraph.cpp | ~1264 |
| T-C3 | Prefab recursive serialization without depth limit — stack overflow on cyclic hierarchies | Prefab.cpp | ~402 |
| T-C4 | EntityEventBus::Send() reference invalidation during callback dispatch — use-after-free | EntityEventBus.cpp | ~32-39 |
| T-C5 | BehaviorTree MoveTo/Patrol division by near-zero distance — NaN/Inf position propagation | BehaviorTreeExecutor.cpp | ~296, ~331 |

### HIGH (10)

| ID | Description | File | Line |
|----|-------------|------|------|
| T-H1 | Spline methods underflow when `m_Points.size()==1` and not closed — usize wraparound | Spline.cpp | ~111-384 |
| T-H2 | AISystem ProcessFlee zero-length direction normalization — Inf | AISystem.cpp | ~407 |
| T-H3 | LevelGenerator SelectPrefab accesses empty `valid` vector — UB | LevelGenerator.cpp | ~237-249 |
| T-H4 | FlashTimeline InsertOrModifyKeyframe returns `.back()` assuming non-empty | FlashTimeline.cpp | ~70 |
| T-H5 | Dialogue legacy mode accesses `dialogueLines[currentLine]` without bounds check after increment | DialogueSystem.cpp | ~173-206 |
| T-H6 | ControllerSystem Quaternion-to-Matrix Shepperd's method divide-by-zero | ControllerSystem.cpp | ~41-60 |
| T-H7 | AudioEventGraph `uniform_int_distribution<usize>` — non-standard, potential data race on static rng | AudioEventGraph.cpp | ~886 |
| T-H8 | ObjectPool Acquire returns stale entity ID if entity was destroyed externally | ObjectPool.cpp | ~44 |
| T-H9 | Prefab LoadPrefab: no JSON array validation before vec3/vec4 indexing | Prefab.cpp | ~620-658 |
| T-H10 | SceneSerializer stoul overflow on 32-bit NodeId | SceneSerializer.cpp | ~2308 |

### MEDIUM (15)

| ID | Description | File |
|----|-------------|------|
| T-M1 | AIBehaviors Patrol waypoint `size()-1` underflow on empty vector | AIBehaviors.cpp |
| T-M2 | Camera2D division by near-zero zoom (0.01 min) | ControllerSystem.cpp |
| T-M3 | Navmesh SmoothPath/StringPull assumes non-empty waypoints | Navmesh.cpp |
| T-M4 | Timeline event tracking doesn't account for reverse playback (PingPong burst-fire) | Timeline.cpp |
| T-M5 | QuestFlow AdvanceQuestFlow modifies activeNodes while iterating snapshot | QuestFlow.cpp |
| T-M6 | Subtitle/Announcer division by zero in fade alpha → negative u8 wrap | SubtitleSystem.cpp, Announcer.cpp |
| T-M7 | DestructibleSystem dereferences m_World without null check | Destructible.cpp |
| T-M8 | DataAsset JSON array access without size validation for vec3/vec4 | DataAsset.cpp |
| T-M9 | SaveSystem GetSlotPath could accept out-of-range slot in edge cases | SaveSystem.cpp |
| T-M10 | DialogueSystem Advance erases iterator then checks m_ActiveEntity (fragile pattern) | DialogueSystem.cpp |
| T-M11 | VisualScriptSystem iterates entity list while scripts may destroy entities — invalidation | VisualScriptSystem.cpp |
| T-M12 | Animation SampleKeyframes returns zero quaternion `T{}` on empty input | Animation.cpp |
| T-M13 | CoroutineScheduler YieldSeconds linear scan for matching context — O(N) per yield | CoroutineScheduler.cpp |
| T-M14 | SceneImporter plays `animations[0]` without empty check | SceneImporter.cpp |
| T-M15 | AudioEventGraph static RNG data race potential (thread_local needed) | AudioEventGraph.cpp |

### LOW (8)

| ID | Description |
|----|-------------|
| T-L1 | Localization Format() O(N*M) complexity with multiple parameters |
| T-L2 | AISystem agent state map grows indefinitely for short-lived entities |
| T-L3 | TweenSystem minimum duration 0.001s allows instant completion |
| T-L4 | FluidSimulation grid reallocation on every size change (no hysteresis) |
| T-L5 | Prefab s_NextId global counter not thread-safe |
| T-L6 | VOXLoader hardcoded array access without buffer bounds checks |
| T-L7 | EditorLayer custom template log format uses `{}` instead of `%s` |
| T-L8 | SubtitleSystem RenderOverlay with zero viewport dimensions |

---

## 3. PERFORMANCE (21 findings)

### P1 — High Impact (6)

| ID | Description | File | Estimated Impact |
|----|-------------|------|-----------------|
| P-1 | SpriteBatchRenderer: `ToEuler()` per sprite for Z rotation — full inverse trig | SpriteBatchRenderer.cpp:484 | 500 sprites → 500 unnecessary Euler decompositions/frame |
| P-2 | SpriteBatchRenderer: double `GetRegion()` lookup per sprite (collect + build) | SpriteBatchRenderer.cpp:363,467 | 500 redundant hash map lookups/frame |
| P-3 | SpriteBatchRenderer: string comparison in sort comparator | SpriteBatchRenderer.cpp:381 | O(N log N) string compares, 5-20x slower than int |
| P-4 | RenderEntity: 6+ optional GetComponent calls per entity (sprite/tilemap/veg/water/text/anim) | RenderSystem.cpp:3551-3631 | 200 entities → 1000+ unnecessary lookups/frame |
| P-5 | Physics2D: `GetRotationZ()` calls `ToEuler()` in 10+ hot paths + Box2DBackend | Physics2D.cpp:57+, Box2DBackend.cpp:29 | 50 bodies → 500+ full Euler decompositions/step |
| P-6 | ControllerSystem: `ToMatrix()` for yaw extraction — full 4x4 matrix construction | ControllerSystem.cpp:844,1010 | 10x more expensive than direct quaternion extraction |

### P2 — Medium Impact (10)

| ID | Description | File |
|----|-------------|------|
| P-7 | AISystem: `ToMatrix()` for forward direction extraction (3 sites) | AISystem.cpp:525,588,720 |
| P-8 | AISystem: `unordered_map::operator[]` default-constructs AgentState with vector | AISystem.cpp:69 |
| P-9 | TweenSystem: per-frame vector allocation with string member for callbacks | TweenSystem.cpp:284 |
| P-10 | TweenSystem: string concatenation per callback invocation for method signature | TweenSystem.cpp:266 |
| P-11 | DialogueSystem: per-frame string construction in SyncDialogueBoxUI (7 strings) | DialogueSystem.cpp:453,502-503 |
| P-12 | DialogueSystem: string concatenation in BroadcastEvent | DialogueSystem.cpp:520-522 |
| P-13 | RenderSystem: IK chain vector allocated per frame per IK entity (always size 3) | RenderSystem.cpp:734 |
| P-14 | EditorLayer: remaining GetAllEntities() calls in 7 editor panel paths | EditorLayer.cpp:2925,3997,etc. |
| P-15 | SpriteBatchRenderer: string copy on texture change between batches | SpriteBatchRenderer.cpp:475 |
| P-16 | ControllerSystem: SurfaceAligned `ToMatrix()` for tangent frame (2 sites) | ControllerSystem.cpp:1549,1667 |

### P3 — Low Impact (5)

| ID | Description | File |
|----|-------------|------|
| P-17 | Weather2D: double iteration over all particles per update | Weather.cpp:238-269 |
| P-18 | AISystem: m_DebugLines grows unbounded (memory only) | AISystem.cpp:54 |
| P-19 | FlowerSystem: per-event vector allocations for breaks/impacts | FlowerSystem.cpp:512,722 |
| P-20 | LevelStreaming: GetAllEntities() before/after for diff (runs only on chunk load) | LevelStreaming.cpp:215,222 |
| P-21 | Physics2D: ToEuler().z then immediately reconstruct quaternion — lossy roundtrip | Physics2D.cpp:380-381 |

### Top 5 Performance Fixes by Impact-to-Effort Ratio

1. **Create `GetRotationZ()` utility** with single `atan2` — fixes P-1, P-5, P-21 (12+ call sites)
2. **Add `Quaternion::GetForward()/GetRight()` helpers** via `Rotate()` — fixes P-6, P-7, P-16 (7 sites)
3. **Cache AtlasRegion pointer + pre-hash texture paths** — fixes P-2, P-3 in sprite batching
4. **Promote per-frame local vectors to members** — fixes P-9, P-13
5. **Pre-classify entities with component bitmask** — fixes P-4 (eliminates 1000+ lookups/frame)

---

## 4. FEATURE WIRING (14 findings)

### HIGH (3)

| ID | Description | Impact |
|----|-------------|--------|
| F-H1 | **AISystem never instantiated** — fully implemented (615+ lines: patrol/chase/flee/navmesh) but never created in PlayMode, Player, or EditorLayer | AI entities are completely inert at runtime |
| F-H2 | **AISystem not wired in Player** — no member, no Update() call | AI broken in exported games |
| F-H3 | **~30 component types have no AngelScript bindings** — Rigidbody, Colliders, TriggerZone, Interactable, Pickup, Inventory, Timer, Lock, Switch, GoalZone, Conveyor, Teleporter, MovingPlatform, etc. | Scripts cannot read/modify these components at runtime |

### MEDIUM (6)

| ID | Description | Impact |
|----|-------------|--------|
| F-M1 | `SetBindingsPluginSystem` never called — plugin AS bindings dead | `Plugin_IsLoaded()` etc. crash or fail |
| F-M2 | `SetBindingsAudioGraphRuntime` never called — audio graph AS bindings dead | `AudioGraph_TriggerEvent()` etc. fail |
| F-M3 | InputActionMap.Update() never called in PlayMode | Remapped input actions stale in editor play mode |
| F-M4 | ~~ScriptPropertyParser never used~~ — **Fixed**: wired into InitScript(), [Header] support added | `[Range]`, `[Header]`, `[Tooltip]` decorators now functional |
| F-M5 | ~20 gameplay component types lack VS nodes (Inventory, Timer, Lock, Switch, etc.) | Visual script designers can't interact with these mechanics |
| F-M6 | .enjshader/.enjaudiopkg/.enjparticle files not packed by BuildPipeline | Graph assets missing from exported games |

### LOW (5)

| ID | Description |
|----|-------------|
| F-L1 | FlashAPIShim bindings never registered with AngelScript |
| F-L2 | TweenRotation VS node type defined but never registered |
| F-L3 | FlowerSystem.SetGameCameraEntity not called in PlayMode |
| F-L4 | CollaborativeEditingSystem has no network transport layer |
| F-L5 | Water3D generates mesh data but has no Vulkan render integration |

---

## 5. API CONSISTENCY (43 findings)

### HIGH (4)

| ID | Description | File | Status |
|----|-------------|------|--------|
| A-H1 | Duplicate `WeatherSystem` class name in `Effects::` and `Weather::` namespaces | Weather.h, WeatherSystem.h | **RESOLVED** — `Weather::WeatherSystem` is dead code (`#if 0`) with comment |
| A-H2 | World injection: 3 incompatible patterns (SetWorld / Initialize(world) / Update(world,dt)) across 40+ systems | Multiple | **ACCEPTED** — 3 patterns are contextual (SetWorld for late-binding, Initialize for one-shot, Update for stateless). Unifying would touch 40+ systems with high regression risk |
| A-H3 | Missing `ENJIN_API` on NetworkSystem and AudioSystem class declarations | NetworkSystem.h, AudioSystem.h | **FIXED** — Added `ENJIN_API` to both class declarations |
| A-H4 | Network auth/replay protection TODOs — critical security features not implemented | NetworkSystem.cpp:384-385 | **RESOLVED** — HMAC-SHA256 authentication + replay window fully implemented |

### MEDIUM (8)

| ID | Description | Status |
|----|-------------|--------|
| A-M1 | `Init()` vs `Initialize()` naming — ScriptEngine alone uses `Init()` | **FIXED** — Renamed to `Initialize()` in header, implementation, and all call sites |
| A-M2 | Non-const getters expose mutable internal state (SceneManager, NodeGraph, FeedbackSystem, etc.) | **ACCEPTED** — Intentional const/non-const overload pattern for editor mutation access |
| A-M3 | PlayMode.h includes 30+ headers — any includer pulls entire engine | **ACCEPTED** — PlayMode owns 25+ subsystems by value; full includes required for member types |
| A-M4 | DialogueTree.h includes full `nlohmann/json.hpp` — use `json_fwd.hpp` in header | **FIXED** — Switched to `json_fwd.hpp` in header; full include moved to .cpp and consumers |
| A-M5 | VulkanContext::Initialize returns bool but RenderSystem::Initialize returns void | **ACCEPTED** — Changing return type requires auditing all callers; low practical impact |
| A-M6 | Audio graph Save/Load not implemented (TODO stubs in menu) | **RESOLVED** — Already implemented (Save/Load fully functional) |
| A-M7 | VSync toggle / swapchain recreation has 3 separate TODO comments | **RESOLVED** — Already implemented (RequestVSyncChange/IsVSyncEnabled) |
| A-M8 | Prefab write ("Apply to Prefab") not implemented | **RESOLVED** — Already implemented (PrefabManager::SavePrefab) |

### LOW (31)

Key items: duplicate WeatherType enums, mixed namespace declaration style, `Destroy()` vs `Shutdown()` naming, 7 magic number categories (PI literals, sentinel values, hardcoded acceleration/sensitivity), mixed `deltaTime`/`dt` naming, redundant QuestSystem::SetWorld() with per-method world parameter, `SoundHandle`/`ChannelHandle` both raw `u32` (interchangeable), PlayMode getters not const-qualified, IPhysicsBackend query methods not const.

**Status:** All 31 LOW items accepted/deferred. These are cosmetic naming conventions, style preferences, and type alias improvements that carry high blast radius for low practical benefit. The codebase is internally consistent within each subsystem.

---

## Recommended Fix Priority

### Must Fix (CRITICAL + HIGH security/stability)
1. **S-C1/S-H8**: Replace `std::system()`/`popen()` on Linux/macOS with `posix_spawn()`
2. **S-C2**: Add auth + enum validation to collaborative editing
3. **S-C3/S-C4**: Add size overflow checks before `static_cast<u16>` in network code
4. **T-C1/S-H5/S-H6**: Add try-catch + range validation to SceneSerializer stoi/stoul
5. **T-C2**: Replace `nodeMap.at()` with `find()` in ShaderGraph codegen
6. **T-C3**: Add depth limit to prefab recursive serialization
7. **T-C4**: Copy listener vector before iterating in EntityEventBus::Send()
8. **T-C5**: Guard BehaviorTree division by near-zero distance
9. **S-H1/S-H2**: Add Vulkan error checks to RT subsystem
10. **F-H1/F-H2**: Wire AISystem into PlayMode and Player

### Should Fix (HIGH features + P1 performance)
11. **F-H3**: Add AngelScript bindings for ~30 component types
12. **P-1/P-5**: Create `GetRotationZ()` utility with single atan2
13. **P-6/P-7**: Add `Quaternion::GetForward()/GetRight()` helpers
14. **P-2/P-3**: Cache AtlasRegion + pre-hash texture paths in sprite batching
15. **F-M1/F-M2**: Wire SetBindingsPluginSystem + SetBindingsAudioGraphRuntime
16. **F-M3**: Call InputActionMap.Update() in PlayMode

### Nice to Fix (MEDIUM)
17. All remaining MEDIUM security findings (WAV/VOX validation, save file type checks)
18. All remaining MEDIUM stability findings (null guards, bounds checks)
19. P2 performance findings (per-frame allocations, string operations)
20. API consistency improvements (naming, const correctness, include hygiene)
